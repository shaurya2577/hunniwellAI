# Innovator Portal Capture

Converts saved **PitchBook** company profile HTML pages → Word (.docx/.doc).

```bash
pip install -r requirements.txt
python pitchbook_converter_ui.py
```

Opens a local web UI—drop the HTML file, convert, download. Pick save location in the browser's download dialog.
