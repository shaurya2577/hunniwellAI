# Copy to .env (gitignored) and fill in.
# Most env vars are only needed by ai/airtable_ingest.

# AI ingest
ANTHROPIC_API_KEY=
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=

# CompanyFiles root. Defaults to ~/Documents/Hunniwell if unset.
HUNNIWELL_COMPANYFILES_ROOT=
